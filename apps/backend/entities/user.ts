/**
 * Backend User Entities
 *
 * Re-exports shared types from the monorepo package
 * to maintain backward compatibility and ensure type consistency.
 */

// Import and re-export all shared types
export * from "@ebuddy/shared-types";
